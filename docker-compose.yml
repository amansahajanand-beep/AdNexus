version: '3.8'

services:
  backend:
    build: ./backend
    container_name: gam-backend
    restart: unless-stopped
    env_file: ./backend/.env
    environment:
      - NODE_ENV=production
      - PORT=3001
      - RUN_IN_PROCESS_WORKERS=false
    volumes:
      - ./backend/logs:/app/logs
    networks:
      - gam-net
    healthcheck:
      test: ["CMD", "wget", "--spider", "-q", "http://localhost:3001/health"]
      interval: 30s
      timeout: 5s
      retries: 3

  gam-worker:
    build: ./backend
    container_name: gam-worker
    restart: unless-stopped
    env_file: ./backend/.env
    environment:
      - NODE_ENV=production
      - GAM_SYNC_CONCURRENCY=4
    command: ["node", "src/worker.js"]
    volumes:
      - ./backend/logs:/app/logs
    networks:
      - gam-net
    depends_on:
      - backend

  frontend:
    build: ./frontend
    container_name: gam-frontend
    restart: unless-stopped
    ports:
      - "80:80"
      - "443:443"
    depends_on:
      - backend
    networks:
      - gam-net
    volumes:
      - ./ssl:/etc/nginx/ssl:ro

networks:
  gam-net:
    driver: bridge
